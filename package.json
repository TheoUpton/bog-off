{
  "name": "bog-off",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "test": "vitest run --config vitest.config.js",
    "test:watch": "vitest --config vitest.config.js",
    "start": "cross-env NODE_ENV=production node server/server.js",
    "dev": "nodemon --watch server --watch public/shared server/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^5.2.1",
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "nodemon": "^3.1.14",
    "vitest": "^4.1.5"
  },
  "vitest": {
    "environment": "jsdom"
  }
}
